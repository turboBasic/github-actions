name: 🌜 apply-ruleset

# A ruleset write has no revert. It runs by hand and dry by default, so the difference always prints
# first. Only `workflow_dispatch` reaches the write step (FR-003). The schedule reads and fails on any
# difference: a hand edit in the UI is otherwise invisible until the next dispatch.
on:
  workflow_dispatch:
    inputs:
      ruleset:
        description: Name of the committed file under .github/rulesets, without the .json suffix.
        required: true
      dry-run:
        description: Print the difference and stop before writing.
        type: boolean
        default: true
  schedule:
    - cron: "17 6 * * 1" # Monday morning, so a week's drift is read at the start of a week

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false

permissions: {}

jobs:
  # The ruleset the dispatch named, or every file in the tree when it named none. `.github/rulesets/`
  # owns which rulesets exist, so the scheduled path reads the directory rather than a hardcoded list.
  which:
    name: which
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read # the checkout, to list the committed files
    outputs:
      rulesets: ${{ steps.list.outputs.rulesets }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # An empty matrix skips the job below, and a skipped job reports success — so a scheduled run that
      # listed nothing would be a green check that read no ruleset at all. It fails here instead.
      - id: list
        uses: $/actions/ruleset-state
        with:
          command: list-rulesets
          named: ${{ inputs.ruleset }}

  apply:
    name: apply
    needs: which
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read # the checkout, to read the committed file
    strategy:
      fail-fast: false # one ruleset's refusal must not hide another's difference
      matrix:
        ruleset: ${{ fromJSON(needs.which.outputs.rulesets) }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - id: token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          client-id: ${{ secrets.RELEASE_APP_CLIENT_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          # The App behind these secrets must hold this, and its installation must have accepted it: a
          # permission added to an App does not reach an installation until the account owner approves.
          permission-administration: write # the only right a ruleset read or write needs (R8)

      # In full, because a list-endpoint summary carries no rules, conditions or bypass_actors.
      # `includes_parents=false` leaves out an organisation's, which this repository cannot write.
      - name: Read the live rulesets in full
        uses: $/actions/ruleset-state
        with:
          command: read-rulesets
          token: ${{ steps.token.outputs.token }}
          repository: ${{ github.repository }}
          live-path: ${{ runner.temp }}/live.json

      - id: decide
        uses: $/actions/ruleset-decisions
        with:
          committed: .github/rulesets/${{ matrix.ruleset }}.json
          live: ${{ runner.temp }}/live.json

      # `always()`, and no `continue-on-error` above: a refusal fails the job where it happens rather
      # than being masked and re-raised. The difference still prints — the decision writes its outputs
      # before exiting non-zero.
      - name: Print the difference
        if: always()
        env:
          DIFFERENCE: ${{ steps.decide.outputs.difference }}
        run: printf '%s\n' "$DIFFERENCE"

      # `inputs.dry-run` is absent on a scheduled run, and an absent input compares equal to `false` —
      # so the event is named first. A schedule reads and never writes (FR-003).
      - name: Write the ruleset
        if: >-
          github.event_name == 'workflow_dispatch' && steps.decide.outcome == 'success' &&
          !inputs.dry-run &&
          (steps.decide.outputs.verdict == 'create' || steps.decide.outputs.verdict == 'update')
        uses: $/actions/ruleset-write
        with:
          token: ${{ steps.token.outputs.token }}
          repository: ${{ github.repository }}
          verdict: ${{ steps.decide.outputs.verdict }}
          ruleset-id: ${{ steps.decide.outputs.ruleset-id }}
          body: ${{ steps.decide.outputs.body }}

      # A dispatch exits successfully on a difference, since that is the reason to dispatch. A scheduled
      # run finding one means GitHub stopped matching the tree with nobody watching.
      - id: drift
        name: Fail on drift
        if: github.event_name == 'schedule' && steps.decide.outputs.verdict != 'nothing'
        env:
          MESSAGE: ${{ steps.decide.outputs.message }}
        run: |
          printf '::error::live ruleset no longer matches the tree: %s\n' "$MESSAGE"
          exit 1
