{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended", "helpers:pinGitHubActionDigests"],
  "reviewers": ["turboBasic"],
  "assignees": ["turboBasic"],
  "schedule": ["every weekend"],
  "semanticCommits": "enabled",
  "semanticCommitType": "chore",
  "semanticCommitScope": "deps",
  "packageRules": [
    {
      "matchUpdateTypes": ["minor", "patch"],
      "groupName": "non-major updates",
      "groupSlug": "non-major"
    },
    {
      "description": "Nothing in this tree names a first-party reference with a ref today — every one is $/. This rule stays anyway: a future step naming another turboBasic repository by tag is meant to track a moving major, and a digest pin here would defeat that on purpose.",
      "matchPackageNames": ["turboBasic/**"],
      "enabled": false
    },
    {
      "description": "This pin version-locks the vendored .specify/ and speckit skills, and only `mise run spec-kit-upgrade` re-syncs them — so the bump is half a change until a human runs it. Grouped alone, never with anything mergeable as-is.",
      "matchDepNames": ["pipx:specify-cli"],
      "groupName": "specify-cli",
      "groupSlug": "specify-cli",
      "prBodyNotes": [
        ":warning: docs/ai-instructions.md's Specs section owns the upgrade procedure — read it before merging this alone."
      ]
    },
    {
      "description": "tests/test_commit_grammar.py holds CZ_VERSION in conventional-commits.yml equal to the commitizen uv.lock resolves. Grouped with lock file maintenance so both move in the one PR, never as a red gate landing separately from the bump that caused it.",
      "matchManagers": ["custom.regex"],
      "matchDepNames": ["commitizen"],
      "groupName": "lock file maintenance",
      "groupSlug": "lock-file-maintenance"
    }
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "The only fact this reads is the CZ_VERSION line; tests/test_commit_grammar.py pre-flights this same pattern against that line, and is what proves the workflow and the lockfile still agree after Renovate moves it.",
      "managerFilePatterns": [".github/workflows/conventional-commits.yml"],
      "matchStrings": ["CZ_VERSION:\\s*[\"']?(?<currentValue>[0-9][0-9A-Za-z.\\-+]*)[\"']?"],
      "depNameTemplate": "commitizen",
      "datasourceTemplate": "pypi"
    }
  ],
  "pre-commit": {
    "enabled": true
  },
  "lockFileMaintenance": {
    "enabled": true,
    "commitMessageAction": "update lock file",
    "schedule": ["every weekend"],
    "groupName": "lock file maintenance",
    "groupSlug": "lock-file-maintenance"
  }
}
